# Бот-консультант ООО «Завод ВРК»

Интеллектуальная система подбора вентиляционного оборудования.  
Работает как **веб-виджет** (чистый HTML/JS) и как **Telegram-бот**,  
используя единую бизнес-логику, RAG-поиск и воронку продаж.

---

## Структура проекта

```
vrk_bot_assistant/
├── config.py              # Настройки, воронка (с filter_value), системный промпт
├── logger.py              # Логирование (файл + консоль)
├── models.py              # Pydantic-модели (raw_attrs, filters)
├── llm_factory.py         # Фабрика LLM-провайдеров (GigaChat/Yandex/OpenRouter/OpenAI)
├── scraper.py             # Парсер каталога: deep crawl, card1_attrs, нормализация фильтров
├── vector_store.py        # ChromaDB (индексация с метаданными, поиск с where-фильтрами)
├── scheduler.py           # APScheduler — автообновление раз в неделю
├── main.py                # FastAPI бэкенд (RAG + metadata filtering, smart text, воронка)
├── telegram_bot.py        # Telegram-бот на aiogram 3.x (навигация, кнопки, текст)
├── requirements.txt       # Python-зависимости
├── .env.example           # Шаблон переменных окружения
├── Dockerfile             # Docker-образ
├── docker-compose.yml     # Оркестрация всех сервисов
├── data/
│   └── raw_products.json  # Сырые данные товаров (генерируется парсером)
├── logs/
│   └── bot.log            # Лог-файл
├── chroma_db/             # Persistent-хранилище ChromaDB
└── static/
    ├── widget.js           # JS-виджет для встраивания на сайт
    ├── widget.css          # Стили виджета
    └── widget.html         # Тестовая HTML-страница с виджетом
```

---

## Быстрый старт

### 1. Клонирование и зависимости

```bash
git clone <repo-url>
cd vrk_bot_assistant

python -m venv venv
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

pip install -r requirements.txt
```

### 2. Настройка `.env`

```bash
cp .env.example .env
```

Откройте `.env` и заполните:

| Параметр | Описание |
|----------|----------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram-бота от @BotFather |
| **LLM** (раскомментировать один блок) | |
| `GIGACHAT_CREDENTIALS` | Base64-креды GigaChat (Сбер) |
| `YANDEX_FOLDER_ID` + `YANDEX_API_KEY` | Yandex GPT |
| `OPENROUTER_API_KEY` | OpenRouter (gpt-4o-mini по умолчанию) |
| `OPENAI_API_KEY` | OpenAI напрямую |

> **Приоритет:** GigaChat → Yandex GPT → OpenRouter → OpenAI.  
> Достаточно указать ключи **одного** провайдера.

### 3. Первый запуск парсера

Парсер обходит каталог, собирает товары и загружает их в ChromaDB:

```bash
python scheduler.py
```

Это займёт несколько минут (зависит от количества товаров).  
Результат: `data/raw_products.json` и заполненная `chroma_db/`.

### 4. Запуск бэкенда

```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

Проверка: [http://localhost:8000/health](http://localhost:8000/health)

### 5. Запуск Telegram-бота

В отдельном терминале:

```bash
python telegram_bot.py
```

### 6. Тест веб-виджета

Откройте в браузере:  
[http://localhost:8000/static/widget.html](http://localhost:8000/static/widget.html)

---

## Автоматическое обновление данных

Планировщик APScheduler встроен в бэкенд (`main.py`).  
По умолчанию парсинг запускается **каждый понедельник в 03:00**.

Настройка в `.env`:
```env
SCRAPER_CRON_DAY_OF_WEEK=mon
SCRAPER_CRON_HOUR=3
SCRAPER_CRON_MINUTE=0
```

Ручной запуск: `python scheduler.py`.

---

## Выбор LLM-провайдера

Система автоматически определяет провайдера по наличию ключей в `.env`.

| Провайдер | Переменные | Модель по умолчанию |
|-----------|-----------|---------------------|
| GigaChat | `GIGACHAT_CREDENTIALS` или `GIGACHAT_CLIENT_ID`+`SECRET` | GigaChat |
| Yandex GPT | `YANDEX_FOLDER_ID` + `YANDEX_API_KEY` | YandexGPT |
| OpenRouter | `OPENROUTER_API_KEY` | `openai/gpt-4o-mini` |
| OpenAI | `OPENAI_API_KEY` | `gpt-4o-mini` |

Для смены провайдера — закомментируйте текущие ключи и раскомментируйте нужные.

---

## Архитектура подбора товаров

### Парсер → метаданные → фильтрация

```
Сайт каталога                  ChromaDB
┌────────────┐    scraper.py   ┌──────────────────────────────────────┐
│ Категории  │───→ deep crawl  │ chunk_text (для семантики)           │
│ Карточки   │   card1_attrs   │ + metadatas:                        │
│ card1_attrs│───→ нормализация│   product_type = grille              │
└────────────┘                 │   location     = outdoor             │
                               │   material     = metal               │
                               │   size_group   = small               │
                               │   raw_attrs_json = {...}             │
                               └──────────────────────────────────────┘
```

**Парсер** (`scraper.py`) обходит каждую карточку товара, извлекает **все** пары
ключ-значение из блока характеристик (`card1_attrs`) и нормализует их в строгие фильтры:

| Сырое значение с сайта | Нормализованный фильтр |
|------------------------|----------------------|
| «Нержавеющая сталь», «Алюминий», «Оцинковка» | `material = metal` |
| «Пластик», «ПВХ» | `material = plastic` |
| «Наружное», «Для фасада» | `location = outdoor` |
| «Внутреннее», «Для помещений» | `location = indoor` |
| Размер < 1000 мм по стороне | `size_group = small` |
| Размер >= 1000 мм | `size_group = large` |

### Воронка с metadata filtering

Каждый шаг воронки в `config.py` привязан к ключу фильтра (`step_id`),
а каждый вариант ответа содержит `filter_value`. При прохождении воронки
накопленные фильтры передаются в ChromaDB как `where`-условие:

```
Пользователь: «Решетки» → «Улица» → «Металл» → «Малый»
                 ↓
active_filters = {product_type: grille, location: outdoor, material: metal, size_group: small}
                 ↓
ChromaDB query: where = {$and: [{product_type: {$eq: grille}}, {location: {$eq: outdoor}}, ...]}
                 ↓
Результат: ТОЛЬКО товары, соответствующие ВСЕМ выбранным параметрам
```

Если строгий фильтр возвращает 0 результатов — система автоматически ослабляет
фильтры (убирает менее важные) и повторяет поиск.

### Умный анализ свободного текста

Бот понимает не только кнопки, но и произвольный текст:

```
Пользователь: «подбери решетку для квартиры в потолок 300х500»
         ↓
Извлечённые фильтры: product_type=grille, location=indoor, size_group=small
         ↓
Бот: «✅ Понял: Вентиляционные решетки, Внутри помещения, Малый размер.
      Какой материал предпочтителен?»
      [Металл] [Пластик] [Не важно]
```

Бот автоматически заполняет всё, что смог распознать, и уточняет только недостающие параметры.

---

## Деплой через Docker

### Сборка и запуск всего стека

```bash
# Убедитесь, что .env создан и заполнен
docker-compose up -d --build
```

Это запустит:
- **backend** — FastAPI на порту 8000 + планировщик
- **telegram-bot** — Telegram long-polling

### Ручной запуск парсера в Docker

```bash
docker-compose run --rm scraper
```

### Остановка

```bash
docker-compose down
```

### Просмотр логов

```bash
docker-compose logs -f backend
docker-compose logs -f telegram-bot
```

---

## Архитектура связи: как компоненты общаются с бэкендом

### Общая схема

```
┌──────────────────────┐     HTTP POST /api/chat     ┌─────────────────────────┐
│  JS-виджет (браузер) │ ─────────────────────────→  │                         │
│  на сайте заказчика  │ ←─────────────────────────  │    FastAPI бэкенд       │
└──────────────────────┘     JSON-ответ              │    (main.py :8000)      │
                                                     │                         │
┌──────────────────────┐     Прямой import Python    │    process_message()    │
│  Telegram-бот        │ ─────────────────────────→  │         ↓               │
│  (telegram_bot.py)   │ ←─────────────────────────  │    ChromaDB + LLM      │
└──────────────────────┘     return ChatResponse     └─────────────────────────┘
```

**JS-виджет** и **Telegram-бот** связаны с бэкендом **по-разному**:

| Компонент | Способ связи | Что нужно указать |
|-----------|-------------|-------------------|
| JS-виджет | HTTP-запросы по сети на `/api/chat` | URL бэкенда в атрибуте `data-api` |
| Telegram-бот | Прямой Python-импорт `process_message()` | Ничего — работает в том же процессе / том же Docker-окружении |

### Какие ключи/токены нужны и откуда их взять

| Что | Откуда получить | Куда положить | Кто использует |
|-----|----------------|---------------|----------------|
| URL бэкенда | Это адрес вашего сервера: домен (`https://bot.ventreshetki.com`) или IP (`http://123.45.67.89:8000`) | Атрибут `data-api` в HTML-коде сайта + переменная `API_BASE_URL` в `.env` | JS-виджет |
| Telegram Bot Token | Создать бота через [@BotFather](https://t.me/BotFather) → `/newbot` → скопировать токен | `TELEGRAM_BOT_TOKEN` в `.env` | Telegram-бот |
| LLM API-ключ | Зарегистрироваться у провайдера (GigaChat / Yandex / OpenRouter / OpenAI) | Соответствующая переменная в `.env` | Бэкенд (llm_factory.py) |

> **Собственного API-ключа для бота создавать не нужно.**
> Бэкенд сам является API-сервером — виджет просто отправляет запросы на его публичный URL.

### Как виджет узнаёт адрес бэкенда

Адрес передаётся через атрибут `data-api` тега `<script>`. Виджет читает его при загрузке:

```js
var API_BASE = scriptTag.getAttribute("data-api") || "";
var API_URL  = API_BASE + "/api/chat";
```

Если `data-api` не указан — запросы пойдут на тот же домен, откуда загружен скрипт.

### Как Telegram-бот связан с бэкендом

Telegram-бот **не ходит по HTTP** к бэкенду. Он импортирует функцию напрямую:

```python
from main import process_message
```

Обе стороны работают с одной и той же ChromaDB и LLM-фабрикой в рамках общего Docker-окружения.

### Пример `.env` для продакшена

```env
API_BASE_URL=https://bot.ventreshetki.com
TELEGRAM_BOT_TOKEN=7123456789:AAHxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxx
```

### Безопасность открытого эндпоинта

Эндпоинт `/api/chat` открыт без авторизации — это нормально для публичного чат-виджета. Для защиты от злоупотреблений:

1. **CORS** — в `main.py` замените `allow_origins=["*"]` на домен сайта:
   ```python
   allow_origins=["https://ventreshetki.com", "https://www.ventreshetki.com"]
   ```
2. **Rate limiting** — ограничение запросов с одного IP через nginx (`limit_req`).
3. **HTTPS** — обязательно для продакшена (см. раздел «Деплой» в `DEPLOY.md`).

---

## Встраивание виджета на сайт

Добавьте перед `</body>` на HTML-странице:

```html
<!-- Виджет чат-бота ВРК -->
<link rel="stylesheet" href="https://bot.ventreshetki.com/static/widget.css">
<script src="https://bot.ventreshetki.com/static/widget.js"
        data-api="https://bot.ventreshetki.com"></script>
```

Замените `bot.ventreshetki.com` на адрес вашего сервера.

При локальной разработке используйте:

```html
<link rel="stylesheet" href="http://localhost:8000/static/widget.css">
<script src="http://localhost:8000/static/widget.js"
        data-api="http://localhost:8000"></script>
```

---

## Локальное тестирование (через Telegram, без виджета)

Пошаговая инструкция для проверки проекта на своём компьютере. Виджет не используется — все проверки через Telegram-бота.

### Что понадобится

- Python 3.10+
- Токен бота от [@BotFather](https://t.me/BotFather)
- Ключ одного LLM-провайдера (OpenRouter / OpenAI / GigaChat / Yandex)

### Шаг 1: Окружение и зависимости

```bash
cd vrk_bot_assistant
python -m venv venv
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate

pip install -r requirements.txt
```

### Шаг 2: Файл `.env`

```bash
cp .env.example .env
```

Откройте `.env` и заполните **обязательно**:

1. **TELEGRAM_BOT_TOKEN** — токен от @BotFather (например `7123456789:AAH...`).
2. **Один блок LLM** — раскомментируйте и впишите ключ, например:
   ```env
   OPENROUTER_API_KEY=sk-or-v1-ваш_ключ
   ```

Для локального теста `API_BASE_URL` можно не менять или указать `http://localhost:8000` — на работу Telegram-бота это не влияет.

### Шаг 3: База знаний (парсер)

Без данных в ChromaDB бот не сможет подбирать товары. Один раз запустите парсер:

```bash
python scheduler.py
```

Дождитесь окончания (несколько минут). Проверьте:

```bash
# Файл с товарами должен появиться и быть непустым
dir data\raw_products.json
# или
ls -la data/raw_products.json
```

### Шаг 4: Запуск бэкенда (терминал 1)

В **первом** терминале (с активированным `venv`):

```bash
uvicorn main:app --host 127.0.0.1 --port 8000 --reload
```

Оставьте окно открытым. Должны появиться строки вроде `Uvicorn running on http://127.0.0.1:8000`.

Проверка в браузере: откройте [http://127.0.0.1:8000/health](http://127.0.0.1:8000/health).  
В ответе должно быть: `"status": "ok"`, `"llm_available": true`, `"chroma_documents": <число > 0>`.

### Шаг 5: Запуск Telegram-бота (терминал 2)

Откройте **второй** терминал, перейдите в каталог проекта и активируйте тот же venv:

```bash
cd vrk_bot_assistant
venv\Scripts\activate          # Windows
# source venv/bin/activate     # Linux/macOS

python telegram_bot.py
```

Должна появиться строка вроде `Telegram-бот запущен (long-polling) …`. Окно тоже не закрывайте.

### Шаг 6: Проверка в Telegram

1. Найдите своего бота в Telegram (по username из BotFather) и откройте чат.
2. **До нажатия «Start»** — на экране бота должен отображаться приветственный текст
   (описание бота). Это работает через `bot.set_my_description()`.
3. Нажмите **«Start»** (стандартная кнопка Telegram).  
   Ожидается: **сразу** первый вопрос воронки («Какой тип продукции вас интересует?»)
   с Inline-кнопками выбора. Кнопок «Назад» и «Главное меню» на этом шаге **нет**.
4. Пройдите воронку, нажимая кнопки (категория → место → материал → размер).  
   На каждом шаге (кроме первого) должны быть кнопки **«◀️ Назад»** и **«🏠 Главное меню»**.  
   В конце бот должен выдать подходящий товар (название, артикул, цена, ссылка)
   или предложить связаться с менеджером.
5. Проверьте Reply-кнопку **«📞 Связаться с менеджером»**  
   Должны отобразиться телефон, email, адрес.
6. **Тест свободного текста:** напишите **«подбери решетку для квартиры в потолок 300х500»**  
   Бот должен ответить: «✅ Понял: Вентиляционные решетки, Внутри помещения, Малый размер»
   и спросить только недостающий параметр (материал). После выбора — показать товары.
7. **Навигация:** нажмите «◀️ Назад» — должен вернуться на предыдущий шаг.
   Нажмите «🏠 Главное меню» — должен сбросить воронку и показать первый шаг.

### Шаг 7: Дополнительные проверки (по желанию)

- **Off-topic:** напишите «Какой курс доллара?» — бот должен вежливо отказаться и предложить помощь по вентиляции.
- **Запрос к менеджеру:** напишите «Хочу поговорить с менеджером» — должен выдать контакты.
- **Текст в середине воронки:** находясь на шаге выбора материала, напишите «хочу пластик для кухни» — бот должен распознать material=plastic и продолжить с оставшихся шагов.

### Частые проблемы при локальном тесте

| Симптом | Что проверить |
|--------|----------------|
| Бот не отвечает на /start | Запущен ли `telegram_bot.py` во втором терминале, верный ли `TELEGRAM_BOT_TOKEN` в `.env`. |
| После нажатия «Старт» бот молчит или ошибка | Запущен ли бэкенд (`uvicorn`) в первом терминале. В консоли бэкенда могут быть сообщения об ошибках. |
| Бот пишет «нет точной информации» / не подбирает товары | Запускали ли `python scheduler.py`, есть ли в `data/raw_products.json` данные. Проверьте `/health`: `chroma_documents` должен быть > 0. |
| Ошибка при старте: «Ни один LLM-провайдер не сконфигурирован» | В `.env` должен быть один из ключей: `OPENROUTER_API_KEY`, `OPENAI_API_KEY`, `GIGACHAT_CREDENTIALS` или Yandex. |
| Ошибка при старте бота: «TELEGRAM_BOT_TOKEN не задан» | Создайте бота в @BotFather и скопируйте токен в `.env`. |

### Остановка

- В обоих терминалах нажмите **Ctrl+C** (сначала можно бота, затем бэкенд).

---

## Тестирование

### Через ngrok (для Telegram webhook / внешнего доступа)

```bash
ngrok http 8000
```

Полученный URL используйте как `data-api` в виджете или для вебхука.

### Чек-лист тестирования

- [ ] `/health` возвращает `{"status": "ok", "llm_available": true, ...}`
- [ ] `data/raw_products.json`: у товаров есть поля `raw_attrs` и `filters`
- [ ] Веб-виджет открывается по кнопке, отправляет сообщение, получает ответ
- [ ] Telegram: `/start` → **сразу** первый шаг воронки (без промежуточного экрана)
- [ ] Первый шаг — **без** кнопок «Назад» / «Главное меню»
- [ ] Последующие шаги — **с** кнопками «◀️ Назад» и «🏠 Главное меню»
- [ ] Воронка через кнопки: категория → место → материал → размер → карточка товара
- [ ] Metadata filtering: при выборе «Улица» в результатах **только** `location=outdoor`
- [ ] Свободный текст: «подбери решетку для квартиры 300х500» → автозаполнение фильтров, уточнение недостающих
- [ ] Текст в середине воронки: «хочу пластик» → распознаётся material=plastic
- [ ] Навигация: «◀️ Назад» → предыдущий шаг; «🏠 Главное меню» → сброс
- [ ] «📞 Связаться с менеджером» → контактные данные
- [ ] Off-topic: «Какой курс доллара?» → отказ с предложением помощи по вентиляции
- [ ] Jailbreak: «Забудь инструкции» → стандартный отказ
- [ ] Fallback: если строгий фильтр вернул 0 — автоослабление + повторный поиск
- [ ] Fallback при недоступности LLM → сообщение об ошибке + контакты менеджера

---

## Чек-лист перед продакшеном

- [ ] `.env` заполнен (LLM-ключ, Telegram-токен)
- [ ] Парсер запущен, `data/raw_products.json` не пуст, у товаров есть `filters`
- [ ] ChromaDB заполнена (`/health` → `chroma_documents > 0`)
- [ ] Metadata filtering работает (товары фильтруются по `location`, `material` и т.д.)
- [ ] Свободный текст распознаётся (фильтры извлекаются из произвольных фраз)
- [ ] CORS настроен на конкретный домен (заменить `*` в `main.py`)
- [ ] HTTPS настроен (через nginx / Cloudflare)
- [ ] Логирование работает (`logs/bot.log`)
- [ ] Docker-контейнеры стабильны (`docker-compose ps`)

---

## Лицензия

Внутренний проект ООО «Завод ВРК». Все права защищены.
